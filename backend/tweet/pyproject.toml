[tool.poetry]
name = "create-tweet"
version = "0.1.0"
description = ""
authors = [":"]
readme = "README.md"
packages = [{include = "create_tweet"}]

[tool.poetry.dependencies]
python = "^3.9"
boto3 = "^1.34.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
